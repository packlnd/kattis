#include<iostream>
#include<vector>
#include<algorithm>
#include<unordered_map>
#include<string>

int main() {
  int T = 0;
  while (std::cin >> T) {
    std::vector<long> nums, idx(T, -1);
    long n = 0;
    std::cin >> n;
    nums.push_back(n);
    long best = -2;
    for (int i=T-2; i>=0; --i) {
      
    }
  }
  return 0;
}
